spring:
  application:
    name: soldo

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/soldo_db}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: ${JPA_SHOW_SQL:false}
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        default_schema: public

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true

  jackson:
    default-property-inclusion: non_null

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

telegram:
  bot:
    secret: ${TELEGRAM_BOT_SECRET:123}
    token: ${TELEGRAM_BOT_TOKEN:123}

vk:
  client:
    id: ${VK_CLIENT_ID:}
    secret: ${VK_CLIENT_SECRET:}

yandex:
  client:
    id: ${YANDEX_CLIENT_ID:}
    secret: ${YANDEX_CLIENT_SECRET:}

server:
  port: ${SERVER_PORT:8080}
  address: 0.0.0.0

logging:
  level:
    root: INFO
    ru.savvy.soldo: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: ${SWAGGER_ENABLED:true}

swagger:
  username: ${SWAGGER_USERNAME:admin}
  password: ${SWAGGER_PASSWORD:admin}

app:
  upload-dir: ${UPLOAD_DIR:./uploads}
  # Базовый публичный URL приложения — используется для построения webhook URL
  # при регистрации Telegram-ботов тенантов.
  public-url: ${APP_PUBLIC_URL:http://localhost:8080}